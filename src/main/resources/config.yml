enabled: true
version: 1.4.1
headUse:
  enabled: true
  cooldown: 20 #cooldown in seconds after expiry
  duration: 60 #duration in seconds
  removeHead: true #remove the head on use
  targetProtection:
    disableDamage: false #Player can not damage a player while invisible to them
    damageCap:
      enabled: false
      capFinalDamage: false #Caps damage after armor, potions, etc.
      value: 14
disguise:
  enabled: true #Enable using disguise heads
  craftingEnabled: true
  cheaperCraft: false
  removeHead: true
  duration: 300 #duration in seconds
lifeSystem:
  enabled: false #If false, disables all other settings in this category
  revives:
    enabled: true
    crafting: true
  extraLives:
    enabled: true
    crafting: true
  maxLives: 3
commands:
  decorate: true #Enable /decorate
  head: true #Enable /head